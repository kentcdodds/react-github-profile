// this file holds singleton state that can be manipulated
// and read as-needed (no subscriptions necessary).
// If components need to be notified of state updates, then
// you should use context.
export default {
  suspenseMaxDuration: 0,
}
